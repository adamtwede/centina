export function resolve_symbol(statement: string) {
  // this function represents a hook into the actual AISL external symbol resolver but in a Typescript file instead of an AISL spec file (such as a .aisl file).
  // both ways are valid
}
