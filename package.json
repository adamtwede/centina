{
  "name": "centina",
  "version": "0.3.2",
  "description": "Centina — spec-flavored TypeScript: type-checked falsework for coding tasks. TypeScript is the grammar; a spec-plane checker (not tsc) is the arbiter of done.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "check": "tsx checker/cli.ts",
    "test": "tsx --test \"checker/**/*.test.ts\" \"scripts/**/*.test.mjs\"",
    "bump-version": "node scripts/bump-version.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "workspaces": [
    "checker"
  ],
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^6.0.3"
  }
}
